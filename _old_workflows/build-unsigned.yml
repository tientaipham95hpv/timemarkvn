name: Build GeoStamp Unsigned IPA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
          xcodebuild -version
          swift --version

      - name: Generate Xcode project
        run: ./generate_project.sh

      - name: Resolve package dependencies
        run: |
          xcodebuild \
            -project GeoStamp.xcodeproj \
            -scheme GeoStamp \
            -sdk iphoneos \
            -configuration Release \
            -showBuildSettings >/dev/null

      - name: Build unsigned app
        run: |
          rm -rf build
          xcodebuild \
            -project GeoStamp.xcodeproj \
            -scheme GeoStamp \
            -sdk iphoneos \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE_SPECIFIER=""

      - name: Package unsigned IPA
        run: |
          set -e
          APP_PATH="build/Build/Products/Release-iphoneos/GeoStamp.app"
          test -d "$APP_PATH"
          rm -rf Payload GeoStamp.ipa
          mkdir Payload
          cp -R "$APP_PATH" Payload/GeoStamp.app
          /usr/bin/zip -qry GeoStamp.ipa Payload
          ls -lh GeoStamp.ipa

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: GeoStamp-Unsigned-IPA
          path: GeoStamp.ipa
          if-no-files-found: error
          retention-days: 14
